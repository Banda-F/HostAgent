services:
  - type: web
    name: hostagent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python render_app.py
    env: python
    plan: free
    healthCheckPath: /health
