services:
  - type: web
    name: hostagent
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn render_app:app -k uvicorn.workers.UvicornWorker
    env: python
    plan: free
    healthCheckPath: /health
